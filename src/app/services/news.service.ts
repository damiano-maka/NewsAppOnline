import { HttpClient } from '@angular/common/http';
import { Injectable, inject } from '@angular/core';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class NewsService {
/*   http = inject(HttpClient);
  private newUrl = 'https://newsapi.org/v2'
  private api ="306021cf39424031b1c3ac021ef1fb9b"

  getHeadLines(categoria : string,lingua: string , size: number):Observable<any>{
   return this.http.get<any>(`${this.newUrl}/everything?q=${categoria}&language=${lingua}&pageSize=${size}&apiKey=${this.api}`,)
  }
 */
  
}
